## 1. Instalação (Dependências)

# Instalação básica
pip install agno

# Instalação para usar Groq
 pip install groq

# Instalação de ferramentas extras comuns (ex: DuckDuckGo para busca)
### DuckDuckGo
pip install ddgs

# Carragar dados da .env
pip install openai python-dotenv


## 2. Configuração de Ambiente (API Keys)

# Crie um arquivo .env e adicione uma chave de API (Grok, OpenAI, Gemine, etc...)

OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxx

