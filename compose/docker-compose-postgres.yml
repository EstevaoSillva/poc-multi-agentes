services:

  postgres:
    image: postgres
    hostname: postgres
    ports:
      - 5434:5432
    environment:
      - POSTGRES_DB=agent
      - POSTGRES_PASSWORD=123456
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:


